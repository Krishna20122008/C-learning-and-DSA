#include<iostream>
using namespace std;
int main(){
    int arr[4];
    cout << arr[2] << endl; // No value is currently assigned by user to the index 2 of the array but some default values called garbage values are already there in those indices..
}